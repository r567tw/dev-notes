

1. Socket 中的通訊端可以被理解為一套用於連結的數字
2. sock 在核心、socket_fd 在使用者空間，socket 層介於核心和使用者空間之間
3. 在作業系統核心空間裡，實作網路傳輸功能的結構是Sock，基於不同的協定和應用場景，會被泛化為各種類型的xx_Sock，他們結合硬體共同實作了網路傳輸的功能。為了將這部分功能曝露給使用者空間的應用程式所用，引入了socket 層，同時將 sock 崁入檔案系統的框架，sock變成一個特殊了一個特殊的檔案，使用者就可以在使用者空間使用檔案控制代碼，也就是 socket_fd 來操作 sock 的網了傳輸能力